<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.castillo.matices.orders">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Orders">
        <activity android:name="com.castillo.matices.orders.sections.products.AddProductActivity"></activity>
        <activity
            android:name="com.castillo.matices.orders.sections.order_detail.OrderDetailActivity"
            android:label="@string/title_activity_order_detail" />
        <activity android:name="com.castillo.matices.orders.sections.add_order.AddOrderActivity" />
        <activity android:name="com.castillo.matices.orders.sections.home.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>